parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/Kernel.php'

    App\Service\AiService:
        arguments:
            $openRouterApiKey: '%env(OPENROUTER_API_KEY)%'
